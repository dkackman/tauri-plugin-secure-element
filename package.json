{
  "name": "tauri-plugin-secure-element-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Tauri Plugin Secure Element - Monorepo",
  "scripts": {
    "dev": "vite",
    "build": "pnpm -r build",
    "install:all": "pnpm install",
    "test": "pnpm -r test",
    "test:rust": "cd tauri-plugin-secure-element && cargo test",
    "test:swift": "cd tauri-plugin-secure-element/ios && (swift test || echo 'Note: Swift tests require iOS simulator. Run from Xcode or use: xcodebuild test -scheme <scheme> -destination \"platform=iOS Simulator,name=iPhone 15\"')",
    "test:android": "cd tauri-plugin-secure-element/android && (./gradlew test || gradle test || echo 'Note: Gradle wrapper not found. Install Gradle or run from Android Studio')",
    "test:android:instrumented": "cd tauri-plugin-secure-element/android && (./gradlew connectedAndroidTest || gradle connectedAndroidTest || echo 'Note: Gradle wrapper not found. Install Gradle or run from Android Studio')",
    "format": "pnpm -r format",
    "format:check": "pnpm -r format:check",
    "format:js": "pnpm -r format:js",
    "format:rust": "pnpm -r format:rust",
    "format:swift": "pnpm -r format:swift",
    "format:kotlin": "pnpm -r format:kotlin",
    "lint": "pnpm -r lint",
    "lint:js": "pnpm -r lint:js",
    "lint:rust": "pnpm -r lint:rust",
    "lint:swift": "pnpm -r lint:swift",
    "lint:kotlin": "pnpm -r lint:kotlin"
  }
}