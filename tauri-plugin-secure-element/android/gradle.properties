# Required so the plugin's Android module can build/test standalone (e.g. `gradle test`
# or `pnpm test:android`). When consumed by a Tauri app, the generated app project
# supplies its own gradle.properties at the build root, so this file only affects
# building this module on its own.
android.useAndroidX=true
