{
  "name": "tauri-plugin-secure-element-api",
  "version": "0.1.0-beta.8",
  "description": "Tauri plugin for secure element use on macOS/iOS (Secure Enclave), Windows (TPM), and Android (Strongbox and TEE).",
  "repository": "https://github.com/dkackman/tauri-plugin-secure-element",
  "license": "Apache-2.0",
  "author": "Don Kackman<dkackman@gmail.com>",
  "homepage": "https://github.com/dkackman/tauri-plugin-secure-element",
  "bugs": "https://github.com/dkackman/tauri-plugin-secure-element/issues",
  "keywords": [
    "tauri",
    "tauri-plugin",
    "secure-element",
    "secure-enclave",
    "strongbox",
    "tee",
    "cryptography",
    "keychain",
    "keystore",
    "mobile",
    "ios",
    "android"
  ],
  "type": "module",
  "types": "./dist-js/index.d.ts",
  "main": "./dist-js/index.cjs",
  "module": "./dist-js/index.js",
  "exports": {
    "types": "./dist-js/index.d.ts",
    "import": "./dist-js/index.js",
    "require": "./dist-js/index.cjs"
  },
  "files": [
    "dist-js",
    "README.md"
  ],
  "scripts": {
    "build": "rollup -c",
    "verify:package": "node scripts/verify-package.mjs",
    "prepublishOnly": "pnpm build && pnpm verify:package",
    "pretest": "pnpm build",
    "test": "pnpm test:rust && (pnpm test:swift || echo '⚠️  Swift tests skipped (require iOS simulator). To run: cd ios && xcodebuild test -scheme tauri-plugin-secure-element -destination \"platform=iOS Simulator,name=iPhone 15\"') && pnpm test:android",
    "test:rust": "cargo test",
    "test:swift": "cd ios && swift test",
    "test:android": "cd android && (./gradlew test || gradle test || echo 'Note: Gradle wrapper not found. Install Gradle or run from Android Studio')",
    "test:android:instrumented": "cd android && (./gradlew connectedAndroidTest || gradle connectedAndroidTest || echo 'Note: Gradle wrapper not found. Install Gradle or run from Android Studio')",
    "format": "prettier --write . && cargo fmt && swiftformat ios/ swift/ 2>/dev/null || echo 'Note: swiftformat not installed. Install with: brew install swiftformat' && pnpm format:kotlin",
    "format:check": "prettier --check . && cargo fmt --check && (swiftformat --lint ios/ swift/ 2>/dev/null || echo 'Note: swiftformat not installed') && pnpm format:check:kotlin",
    "format:js": "prettier --write .",
    "format:rust": "cargo fmt",
    "format:swift": "swiftformat ios/ swift/ || echo 'Note: swiftformat not installed. Install with: brew install swiftformat'",
    "format:kotlin": "pnpm exec ktlint -F 'android/src/**/*.kt'",
    "format:check:kotlin": "pnpm exec ktlint 'android/src/**/*.kt'",
    "lint": "cargo clippy -- -D warnings && (swiftlint lint --no-cache 2>/dev/null || (command -v swiftlint >/dev/null 2>&1 || echo 'Note: swiftlint not installed. Install with: brew install swiftlint')) && pnpm lint:kotlin",
    "lint:rust": "cargo clippy -- -D warnings",
    "lint:swift": "swiftlint lint --no-cache || (command -v swiftlint >/dev/null 2>&1 || echo 'Note: swiftlint not installed. Install with: brew install swiftlint')",
    "lint:kotlin": "pnpm exec ktlint 'android/src/**/*.kt'"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1"
  },
  "devDependencies": {
    "@naturalcycles/ktlint": "1.16.1",
    "@rollup/plugin-typescript": "^12.3.0",
    "@rollup/rollup-win32-arm64-msvc": "^4.62.2",
    "rollup": "^4.62.2",
    "tslib": "^2.8.1",
    "typescript": "^6.0.3"
  }
}
